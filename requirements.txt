# sneaky — requirements
#
# IMPORTANT: install PyTorch in two separate steps, not with a single
# `pip install -r requirements.txt`, or pip may give you the CPU build
# instead of the CUDA one:
#
#   1) pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   2) pip install -r requirements.txt
#
# Requires an NVIDIA GPU with recent drivers (no need to install the
# CUDA Toolkit separately — the PyTorch wheel already bundles it).
# Without an NVIDIA GPU, the app still runs but very slowly (not tested
# on CPU).
#
# After installing the packages below, also download the spaCy model:
#   python -m spacy download en_core_web_sm

open_clip_torch
transformers
accelerate
streamlit
plotly
scikit-learn
scipy
numpy
pandas
pillow
pillow-heif
spacy
umap-learn
ImageHash
reportlab
matplotlib
PyWavelets
